# Server
PORT=5000
NODE_ENV=development
# Note: JWT_EXPIRES_IN is not read in code by default, but you can set it for reference
JWT_SECRET=change_me_super_secret

# Database (used by docker-compose and app)
DB_HOST=postgres
DB_PORT=5432
DB_USER=Vaybe_youdego
DB_PASSWORD=change_me_db_password
DB_NAME=youdego_db

# WhatsApp Cloud API (optional; if not set, app logs OTP in container logs for tests)
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_TEMPLATE_NAME=otp_code
WHATSAPP_TEMPLATE_LANG=fr
